// play with error object
// error when username doesn't exist
// error when UNQ is violated
// error when foreign key constraint is violated
// error when type casting string to number happen
// error when CHK is violated

// play with timestamp and number (expiresAt)
// play with date
